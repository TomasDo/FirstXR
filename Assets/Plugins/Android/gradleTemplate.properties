org.gradle.jvmargs=-Xmx**JVM_HEAP_SIZE**M
org.gradle.parallel=true
unityStreamingAssets=**STREAMING_ASSETS**
**ADDITIONAL_PROPERTIES**

# XREAL SDK 3.1.0 ships nr_common and nr_loader with the same nrsdk.pack namespace.
# Preserve the pre-AGP-9 behavior until XREAL provides distinct library namespaces.
android.uniquePackageNames=false
